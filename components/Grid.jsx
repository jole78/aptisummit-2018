import React from "react";
import styled from "styled-components";

export const CenterInGrid = styled.div`
    margin: auto;
    border: 3px solid green;
    padding: 20px;
    color: white;
`;

const Grid = styled.div`
    height: 100%;
    display: grid;
`;

export default Grid;
