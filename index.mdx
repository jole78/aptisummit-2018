export { default as theme } from "./theme";
import { Split } from 'mdx-deck/layouts'


# CSS
## -- roligare än någonsin --

---
```css
    .parent {
        position: relative;
    }
    .center {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

```
---
import {Center} from "./components"
import {Relative} from "./components/Position.jsx"

<Relative>
    <Center>I'm in the CENTER of attention</Center>
</Relative>

---
## FLEXBOX

---
```css
    .center {
        display: flex;
        justify-content: center;
        align-items: center;
    }

```
---
import FlexBox from "./components/FlexBox.jsx"

<FlexBox>
    I'm in the CENTER of attention
</FlexBox>

---
## GRID

---
```css
body, html {
  height: 100%;
  display: grid;
}
.center { /* thing to center */
  margin: auto;
}
```
---
import Grid, {CenterInGrid} from "./components/Grid.jsx"

<Grid>
    <CenterInGrid>I'm in the CENTER of attention</CenterInGrid>
</Grid>

---
## AUTO PREFIXER

---
![](/assets/autoprefixer.png)

---
## DU SKRIVER 4 RADER CSS

```css
    display: grid;
    transition: all .5s;
    user-select: none;
    background: linear-gradient(to bottom, white, black);
```

---
```css
    display: -ms-grid;
    display: grid;
    -webkit-transition: all .5s;
    -o-transition: all .5s;
    transition: all .5s;
    -webkit-user-select: none;
       -moz-user-select: none;
        -ms-user-select: none;
            user-select: none;
    background: -webkit-gradient(linear, left top, left bottom, from(white), to(black));
    background: -webkit-linear-gradient(top, white, black);
    background: -o-linear-gradient(top, white, black);
    background: linear-gradient(to bottom, white, black);    
```
---




